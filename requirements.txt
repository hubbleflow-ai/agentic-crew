# Crew · unified Python requirements
# Used by control plane, mock MCP servers, and agent containers.
#
# Versions pinned to known-working combinations as of 2026-06-11.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Agent framework + LLM · DeepAgents (LangChain) on Gemini
deepagents>=0.1.0
langchain-google-genai>=2.0
langchain-core>=0.3
langgraph>=0.2

# HTTP + async
httpx>=0.27.0
websockets>=13.0

# Inter-agent messaging
redis[hiredis]>=5.1.0

# Database (for persistence + audit log)
sqlalchemy>=2.0.30
psycopg[binary,pool]>=3.2.0
asyncpg>=0.30.0

# Container management
# Used by:
#   apps/control_plane     · spawns agent containers
#   mocks/mcp_sandbox      · spawns per-task sandbox containers (Option B · real CodeAct)
docker>=7.1.0

# Real browser (Option B · mcp_browser uses real Chromium via Playwright)
# Note: requires `playwright install chromium` in Dockerfile to fetch browser binaries.
playwright>=1.49.0

# Observability · Phoenix tracing (every Gemini/LangGraph call + HTTP hop)
arize-phoenix-otel>=0.6
openinference-instrumentation-langchain>=0.1
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0

# Utility
pydantic>=2.9.0
python-multipart>=0.0.12
PyYAML>=6.0.2
