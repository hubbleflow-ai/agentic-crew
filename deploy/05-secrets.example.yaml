# Template only · the real Secret is created from .env by scripts/k8s-secrets.sh
# and is never committed. Copy this if you prefer to manage it by hand.
apiVersion: v1
kind: Secret
metadata:
  name: crew-secrets
  namespace: crew
type: Opaque
stringData:
  GOOGLE_API_KEY: "replace-me"
  E2B_API_KEY: ""
  LANGSMITH_API_KEY: ""
